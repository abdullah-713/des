# تقرير تقني شامل: نظام "صرح انضباط" لإدارة الحضور والانصراف

## جدول المحتويات
1. [الملخص التنفيذي](#الملخص-التنفيذي)
2. [قائمة الأنظمة](#1-قائمة-الأنظمة)
3. [تحليل الأنظمة](#2-تحليل-الأنظمة)
    - [1. نظام إدارة الحضور والانصراف الذكي](#1-نظام-إدارة-الحضور-والانصراف-الذكي)
    - [2. نظام إدارة الموظفين والهيكل التنظيمي](#2-نظام-إدارة-الموظفين-والهيكل-التنظيمي)
    - [3. محرك القواعد والمعادلات الديناميكية](#3-محرك-القواعد-والمعادلات-الديناميكية)
    - [4. نظام التحكم في الوصول والأدوار (RBAC)](#4-نظام-التحكم-في-الوصول-والأدوار-rbac)
    - [5. نظام التدقيق والأمان](#5-نظام-التدقيق-والأمان)
4. [توثيق منطق العمل (Business Logic)](#3-توثيق-منطق-العمل-business-logic)
5. [الملاحق الفنية](#الملاحق-الفنية)

---

## الملخص التنفيذي
يقدم هذا التقرير تحليلاً تقنياً عميقاً لنظام "صرح انضباط"، وهو منصة متكاملة لإدارة الموارد البشرية تركز على أتمتة عمليات الحضور والانصراف. يتميز النظام بمرونة عالية بفضل محرك القواعد الديناميكي الذي يسمح بتخصيص منطق حساب النقاط والجزاءات دون تعديل الكود المصدري. يعتمد النظام على معمارية PHP Native مع قاعدة بيانات MySQL، مما يضمن أداءً عالياً وسهولة في النشر والصيانة.

---

## 1. قائمة الأنظمة

| رقم النظام | اسم النظام | الرمز التعريفي | الوصف المختصر |
|------------|------------|----------------|---------------|
| 1 | نظام إدارة الحضور والانصراف الذكي | `SYS_ATTENDANCE` | إدارة تسجيل الدخول/الخروج وحساب التأخيرات والمكافآت. |
| 2 | نظام إدارة الموظفين والهيكل التنظيمي | `SYS_EMPLOYEE` | إدارة ملفات الموظفين، الفروع، والبيانات الشخصية. |
| 3 | محرك القواعد والمعادلات الديناميكية | `SYS_RULES` | معالجة الصيغ الرياضية المخصصة لحساب النقاط والجزاءات. |
| 4 | نظام التحكم في الوصول والأدوار | `SYS_RBAC` | إدارة الصلاحيات وتوزيع الأدوار (Super Admin, Manager). |
| 5 | نظام التدقيق والأمان | `SYS_SECURITY` | تسجيل العمليات الحساسة، النسخ الاحتياطي، والحماية من الهجمات. |

---

## 2. تحليل الأنظمة

### 1. نظام إدارة الحضور والانصراف الذكي
نظام مركزي لمعالجة حركات الموظفين اليومية وتحويلها إلى بيانات قابلة للقياس (نقاط/جزاءات).

#### 1.1 المزايا الرئيسية
1. **1.1.1 التسجيل الذكي (Smart Check-in):** التعرف التلقائي على حالة الموظف (حضور/انصراف) بناءً على الوقت أو الوضع العام للنظام.
2. **1.1.2 الحساب الفوري للنقاط:** تقييم فوري للتأخير أو التبكير فور تسجيل الحركة وتحديث رصيد الموظف.
3. **1.1.3 اللوحة الحية (Live Dashboard):** عرض فوري لحالات الحضور ونسب الالتزام لكل فرع.

#### 1.2 الخصائص التقنية
*   **نقطة النهاية (Endpoint):** `attendance_api.php`
*   **نوع الطلب:** `POST (JSON)`
*   **زمن الاستجابة:** < 200ms
*   **التزامن:** يدعم طلبات متزامنة مع قفل (Row-level Locking) على جداول الحضور لمنع تضارب البيانات.

#### 1.3 المتطلبات الفنية
*   PHP 7.4+
*   MySQL 5.7+ (دعم JSON غير إلزامي ولكن مفضل)
*   Client-side Geolocation (اختياري للمستقبل)

---

### 2. نظام إدارة الموظفين والهيكل التنظيمي
قاعدة البيانات المركزية للموارد البشرية.

#### 2.1 المزايا الرئيسية
1. **2.1.1 الملف الشخصي الديناميكي:** إمكانية تخصيص مواعيد عمل مختلفة لكل موظف (تجاوز الإعدادات العامة).
2. **2.1.2 التوليد التلقائي للأكواد:** خوارزمية لتوليد أرقام وظيفية فريدة تعتمد على الاسم.
3. **2.1.3 إدارة الفروع:** ربط الموظفين بمواقع جغرافية/إدارية محددة.

#### 2.2 الخصائص التقنية
*   **الجدول الرئيسي:** `employees`
*   **العلاقات:** `One-to-Many` مع `branches` و `attendance_records`.
*   **حقول مخصصة:** `custom_check_in_start`, `custom_check_in_end` (Nullable Time).

---

### 3. محرك القواعد والمعادلات الديناميكية
العقل المدبر للنظام الذي يسمح بتعريف منطق الأعمال كبيانات وليس كود.

#### 3.1 المزايا الرئيسية
1. **3.1.1 محرر المعادلات (Formula Editor):** واجهة لكتابة صيغ رياضية (مثل Excel) لحساب الخصومات.
2. **3.1.2 المتغيرات المفتوحة:** دعم متغيرات مثل `delay_minutes`, `grace_period` داخل المعادلات.
3. **3.1.3 التنفيذ الآمن:** بيئة معزولة (Sandboxed Eval) لتنفيذ المعادلات ومنع حقن الكود.

#### 3.2 الخصائص التقنية
*   **الفئة المسؤولة:** `RuleEngine` في `config.php`
*   **التخزين:** جدول `dynamic_rules` (عمود `equation` كنص).
*   **الأمان:** استخدام `preg_replace` للسماح فقط بالأرقام والعمليات الحسابية `[0-9\+\-\*\/\(\)\?\:]`.

---

### 4. نظام التحكم في الوصول والأدوار (RBAC)
نظام أمان طبقي يحدد من يمكنه فعل ماذا.

#### 4.1 المزايا الرئيسية
1. **4.1.1 أدوار متعددة المستويات:** إمكانية إنشاء أدوار لا نهائية بصلاحيات مخصصة.
2. **4.1.2 صلاحيات دقيقة (Granular Permissions):** التحكم على مستوى الإجراء (مثل `user_view`, `rules_manage`).
3. **4.1.3 وراثة الصلاحيات:** (مخطط مستقبلي) إمكانية وراثة الأدوار.

#### 4.2 الخصائص التقنية
*   **الجداول:** `roles`, `permissions`, `role_permissions`, `users`.
*   **الدالة المحورية:** `hasPermission($code)` في `config.php`.

---

### 5. نظام التدقيق والأمان
الصندوق الأسود للنظام.

#### 5.1 المزايا الرئيسية
1. **5.1.1 سجل غير قابل للتعديل:** تسجيل جميع العمليات الحساسة في جدول `audit_logs`.
2. **5.1.2 تتبع التغييرات:** حفظ القيمة القديمة والجديدة عند التعديل (`Snapshot`).
3. **5.1.3 النسخ الاحتياطي:** أداة لتصدير قاعدة البيانات واستعادتها.

---

## 3. توثيق منطق العمل (Business Logic)

### 3.1 عملية تسجيل الحضور (`OP_CHECK_IN`)

**الوصف:** العملية الأساسية لإثبات تواجد الموظف.

**تدفق العمليات:**
1. **استلام الطلب:** التحقق من `employee_code` وصحة التوكن.
2. **التحقق من الحالة:**
   - هل النظام مفعل؟ (`attendance_enabled`)
   - هل الوضع الحالي يسمح بالحضور؟ (`attendance_mode`)
   - هل الموظف نشط؟
3. **التحقق من التكرار:** هل يوجد سجل لهذا اليوم؟
   - نعم -> إرجاع خطأ (إلا إذا كان سجل تلقائي فارغ).
   - لا -> إنشاء سجل جديد.
4. **حساب التوقيت:**
   - وقت الموظف المتوقع = `employee.start_time` (أو المخصص).
   - التأخير = `CurrentTime - ExpectedTime`.
   - التبكير = `ExpectedTime - CurrentTime`.
5. **تطبيق القواعد (Rule Engine):**
   - استدعاء `RuleEngine::evaluate('late_deduction')` مع متغيرات التأخير.
   - حساب نقاط المكافأة للتبكير.
6. **الحفظ والتحديث:**
   - إدراج السجل في `attendance_records`.
   - تحديث `employees.points_balance` (خصم الجزاء + إضافة المكافأة).

**المدخلات:** `employee_code`
**المخرجات:** كائن JSON يحتوي على حالة التسجيل، وقت الحضور، والنقاط المحسوبة.
**الاستثناءات:** محاولة تسجيل حضور في وقت الانصراف (يتم رفضها).

---

### 3.2 عملية حساب نقاط الخصم (`OP_CALC_DEDUCTION`)

**الوصف:** المنطق الديناميكي لتحديد العقوبة.

**القواعد المنطقية:**
1. جلب فترة السماح `grace_period` من الإعدادات.
2. تحميل المعادلة النشطة لقاعدة `late_deduction`.
3. **السيناريو الافتراضي (Hardcoded Fallback):**
   - إذا التأخير <= فترة السماح -> 0 نقطة.
   - إذا التأخير <= فترة السماح + 10 -> `penalty_1`.
   - إذا التأخير <= فترة السماح + 20 -> `penalty_2`.
   - ... وهكذا.
4. **السيناريو الديناميكي (Rule Engine):**
   - تنفيذ المعادلة المخزنة: `delay_minutes > 30 ? 50 : 0`.

---

### 3.3 عملية إدارة الصلاحيات (`OP_AUTH_CHECK`)

**الوصف:** بوابة المرور لأي إجراء إداري.

**الخوارزمية:**
1. هل المستخدم مسجل دخول؟ (`session_check`).
2. هل المستخدم `Super Admin`؟ -> السماح فوراً.
3. جلب `role_id` للمستخدم.
4. الاستعلام في `role_permissions` عن `permission_code` المطلوب.
5. النتيجة: `True` (سماح) أو `False` (حجب 403).

---

## الملاحق الفنية

### ملحق أ: معايير الجودة والأداء
1. **الأمان:**
   - تعقيم جميع المدخلات باستخدام `sanitizeInput`.
   - استخدام `PDO Prepared Statements` لمنع SQL Injection.
   - حماية الجلسات من الاختطاف (`Session Fixation Protection`).
2. **الأداء:**
   - استخدام الفهارس (`Indexes`) على أعمدة البحث (`employee_code`, `date`).
   - تقليل استعلامات `JOIN` المعقدة في واجهات الموظفين.
3. **الموثوقية:**
   - التعامل مع الأخطاء عبر `try-catch` وتسجيلها في `error.log`.
   - ضمان سلامة البيانات عبر المعاملات (`DB Transactions`) في العمليات الحساسة.
