# 🚀 آلية النشر وبيانات الدخول

## 1. آلية النشر (Deployment)

تم تحديث ملفات النظام لتشمل ملفات الترقية والملفات الجديدة. قم باتباع الخطوات التالية لرفع التعديلات إلى السيرفر:

### أ) باستخدام السكربت الآلي (Windows)
1. قم بتشغيل الملف `CLICK_TO_DEPLOY.bat` أو `DEPLOY_NOW.bat`.
2. سيقوم النظام بجمع الملفات المطلوبة في مجلد `dist` ورفعها تلقائياً إذا كانت أدوات SSH متوفرة.

### ب) الرفع اليدوي (Manual)
1. قم بجمع الملفات التالية (أو استخدم ما في مجلد `dist` بعد تشغيل سكربت التجهيز):
   - `index.php`
   - `profile.php`
   - `admin.php`
   - `logout.php`
   - `upgrade_users.php` (مهم جداً للترقية)
2. ارفعها إلى مجلد `public_html` في الاستضافة (Hostinger) واستبدل الملفات القديمة.

---

## 2. ما بعد النشر (Post-Deployment) - **خطوة هامة جداً**
بعد رفع الملفات، يجب تشغيل سكربت الترقية لإنشاء جدول المستخدمين ونقل البيانات:

1. افتح المتصفح واذهب إلى الرابط التالي (استبدل sarh.online بدومين موقعك إذا اختلف):
   `https://sarh.online/upgrade_users.php`
2. اضغط على زر **"بدء الترقية الآن"**.
3. ستظهر رسالة نجاح بأنه تم إنشاء جدول `users` ونقل الموظفين.
4. **بعد الانتهاء**، يفضل حذف ملف `upgrade_users.php` من السيرفر للأمان.

---

## 3. بيانات الدخول (Login Credentials)

تحول النظام الآن ليعتمد على المستخدمين (`Users`) بدلاً من جدول الموظفين فقط.

### 👮‍♂️ المدير العام (Admin)
- **اسم المستخدم:** `admin`
- **كلمة المرور:** `admin123` (بيانات افتراضية إذا لم يكن الحساب موجوداً، وإلا تبقى القديمة).

### 👤 الموظفون والمستخدمون
تم نقل جميع الموظفين الحاليين تلقائياً. بيانات دخولهم هي:

- **اسم المستخدم:** هو نفسه **"رقم الموظف"** (Code).
- **كلمة المرور:** 
  - إذا كان للموظف كلمة مرور سابقة: **نفس كلمة المرور القديمة**.
  - إذا لم يكن له كلمة مرور: تم تعيين كلمة المرور الافتراضية لتكون **نفس رقم الموظف**.

### 📱 صفحة الموظف (Profile)
- عند دخول الموظف، سيتم توجيهه تلقائياً إلى صفحة الملف الشخصي الجديدة `profile.php` التي تطابق التصميم القديم ولكن بخصائص المستخدم الجديد.
